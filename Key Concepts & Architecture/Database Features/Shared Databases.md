# Shared Databases and Functions

<br/>

<pre>
You can use the <span class="sf-blue">GROUP BY</span>, <span class="sf-blue">DISTINCT</span>, and <span class="sf-blue">ORDER BY</span> 
clauses on shared databases. 
</pre>